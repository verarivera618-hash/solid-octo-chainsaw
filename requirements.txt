# Local Trading System - No External API Dependencies
# Core Python packages for data analysis and computation
pandas>=2.0.0
numpy>=1.24.0
python-dotenv>=1.0.0

# Testing framework
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-mock>=3.11.0

# Optional: For enhanced data visualization (local only)
matplotlib>=3.7.0
seaborn>=0.12.0

# Note: Removed external API dependencies:
# - alpaca-py (replaced with local simulation)
# - requests (no external API calls)
# - websockets (replaced with local streaming simulation)
# - asyncio-mqtt (not needed for local operation)